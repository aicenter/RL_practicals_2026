# Week 1: Foundations

Goal: build intuition for the basic building blocks of RL: agent, environment,
observation, action, reward and episode, and the Markov property.

0. **Setup.** Follow the [main README](../README.md) and make sure
   `uv run python check_setup.py` passes.
1. **Be the agent.** Interact with the Pacman environment by hand from a
   Python console using `env.reset()` and `env.step(action)`. *(TODO)*
2. **Build an environment.** Implement a simple custom Gymnasium environment
   ([guide](https://gymnasium.farama.org/introduction/create_custom_env/)),
   train an off-the-shelf algorithm on it and compare sparse and dense
   rewards. *(TODO)*
